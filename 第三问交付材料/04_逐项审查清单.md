# 第三问逐项审查清单

| 审查项 | 结论 | 证据 | 论文或提交动作 |
|---|---|---|---|
| 是否直接回答干燥时间 | 通过 | 理论临界时间 57.172706 h，认证时间 57.25 h | 正文区分连续事件根与提交时间 |
| 是否检查所有位置 | 通过 | 事件函数采用 \(\max_r C(r,t)\)，控制点为中心 | 不得用平均值或表面值替代 |
| 题给扩散系数是否正确 | 通过 | 代码采用 `2.4e-3*exp(-0.45/C)*exp(-3850/T)`，温度用 K | 论文公式与代码保持一致 |
| 变系数项是否遗漏 | 通过 | 有限体积界面使用调和平均并以完整通量离散 | 在数值方法中说明守恒通量 |
| 时间积分与论文是否一致 | 通过 | 实际使用 BDF 自适应隐式积分 | 不再写显式 Euler |
| 阈值是否只是按 60 s 扫描 | 通过 | 连续事件函数定位首次过零 | 可写“事件根定位”，不写二分法 |
| 事件后报表是否外推 | 通过 | 保存事件状态，关闭终止事件后续积分到 206100 s；采样函数检查有效积分区间 | 不得调用终止区间外的连续解 |
| 网格是否足够 | 通过 | 1281 与 2561 节点相差 39.34 s，约 0.019% | 正文报告相邻细网格比较 |
| 守恒检查 | 通过 | 最大相对平衡残差 5.27×10⁻⁷ | 称为离散方程平衡，不称实测质量守恒 |
| `result3.xlsx` 格式 | 通过 | 60–206100 s、60 s 间隔；0–2 cm、0.1 cm 间隔；四位小数 | 作为竞赛附件提交 |
| 4 h 后环境处理 | 有条件通过 | 保持附件 1 最后值 | 必须标注为边界延拓假设 |
| 外部实验验证 | 未完成 | 无药材内部温湿实测 | 只能列为局限，不能虚构拟合优度 |
| 与第四问边界 | 通过 | 本问固定半径，不使用附件 2 | 不把第三问时长当作收缩模型结果 |

## 最终结论

旧审查中“题给扩散系数被改写、变系数通量遗漏、显式 Euler 与文档不一致、没有事件定位、终止事件后使用区间外连续解”等问题，均已在当前 Python 版本中修复。第三问已经达到代码、结果表、完整 Excel 和论文表达的交付条件；剩余限制属于需要在论文中明确声明的模型假设和外部验证缺失。

