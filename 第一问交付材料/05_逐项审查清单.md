# 第一问逐项审查清单

| 审查项 | 结论 | 证据 | 提交前动作 |
|---|---|---|---|
| 问题目标是否被直接回答 | 通过 | 已给出指定 7 个时刻×5 个位置的两张论文表，并提供 0–1800 s、1 s×0.1 cm 的 `result1.xlsx` | 论文正文必须实际插入表 1、表 2 |
| 符号是否一致 | 有条件通过 | 代码和公式均以 \(T,C,D,h,h_m\) 表达；但“水分浓度”与“干基含水率”概念混用 | 首次定义后统一写“干基含水率 \(C\)（kg/kg）” |
| 单位是否一致 | 有一处需修正 | 温度、长度、热物性和传质参数的 SI 换算一致；Excel 按 °C、s、cm、kg/kg 输出 | 把“单位长度水分质量”改为“含水率场面积积分”，或补充质量密度后重定义 |
| 公式与代码是否一致 | 通过 | 代码实现圆柱径向传热/变扩散系数传质、中心对称和表面对流边界，参数与附录 2 一致 | 论文公式明确写出初边值条件和 \(D(C)\) |
| 代码与结果是否一致 | 通过 | 当前代码复算的 9 个输出与正式输出 SHA-256 完全一致 | 保留环境与依赖说明 |
| 图表与结果是否一致 | 通过但标签建议修改 | 图中 1800 s 端点与 CSV/JSON 一致，曲线方向与数据一致 | 右图纵轴建议改为“干基含水率（kg/kg）” |
| 创新点是否有基线支撑 | 不通过 | 只有网格收敛、守恒和敏感性，没有与有限差分、解析解或简化模型比较 | 降级为“方法特点”；若要称创新，补充独立基线 |
| 是否存在未经标注的假设或虚构内容 | 存在未标注假设；未发现虚构结果 | 一维径向、忽略端面、固定半径、均质各向同性、常物性、无热湿耦合等主要由代码隐含 | 在“模型假设”集中列明；不得把模拟输出称为实测值 |

## 高优先级修改顺序

1. 补写模型假设，尤其说明 25 cm 长度未进入模型是因为忽略轴向与端面效应。
2. 统一把 \(C\) 表述为干基含水率，避免 kg/kg 与体积浓度混淆。
3. 修正守恒量名称。
4. 把“创新”改为“有限体积守恒离散、刚性 BDF 求解和多重数值检验的组合特点”，除非新增独立基线。
5. 在敏感性分析中注明采用 321 节点网格。
